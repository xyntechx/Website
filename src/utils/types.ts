export interface IExperience {
    role: string;
    organisation: string;
    startDate: string;
    endDate: string;
    responsibilities: string[];
}

export interface IProject {
    name: string;
    description: string;
    url: string;
}
